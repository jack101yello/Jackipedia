# Indiscrete Topology

For a set $X$, the indiscrete topology (also called the trivial topology) is the [[Topology|topology]] on $X$ containing only the empty set and $X$ itself. It is the coarsest topology on $X$.