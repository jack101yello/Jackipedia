# Poset Category

A partially-ordered (poset) set category is a [[Category]] for which there is at most one morphism between any pair of elements, thereby defining some notion of ordering on the objects of the category.

The simplest example of a poset category is that whose objects are the real numbers and whose morphisms are $\leq$ relations.